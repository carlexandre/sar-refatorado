#!/bin/sh
# The sole sudo entry point. Never accepts paths, flags, shell fragments or environment input.
set -eu
[ "$#" -eq 1 ] || exit 64
case "$1" in ''|*[!0-9]*) exit 64;; esac
[ "${#1}" -le 10 ] || exit 64
exec /usr/bin/systemctl start --no-block "sar-worker@$1.service"
